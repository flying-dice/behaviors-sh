# TODO

- [x] Remove the run button from the tree editor — deleted the Play button, `onRun` prop, and import from `BehaviourCanvasToolbar.svelte` and removed the no-op threading from `TreeEditor.svelte`.
- [x] Tree preview should reflect actual tree structure — `shapeFromTree()` in `marketplace-data.ts` now walks the real `BehaviourNode` tree and produces the `Shape` structure. `TreeCard.svelte` uses `getTree()` + `shapeFromTree()` instead of the old `synthShape()` LCG-random generator.
- [x] Import a single tree from a YAML or JSON file — added `yaml` dependency, `parseTreeFile`/`treeIdFromNode` in `serialize.ts`, `pickTreeFile` in `device.ts`, and "Import tree…" action + dialog in `FileMenu.svelte`. Validates against `BehaviourNodeSchema`, prompts for tree ID.
- [x] Refine copy for inputs in the Node inspector — updated to friendly/guiding tone: Name helper expanded, Description gets placeholder "What does this node do?", Retries label simplified with helper text, step textareas get kind-specific placeholders.
- [x] Properly differentiate accent colors across all node types — TreeCard passes root type color to ShapeThumb instead of hardcoded cyan. OutlineRow uses centralized `kindOf()`/`KIND_META`/`nodeColor()`. BehaviourInspector section headings now show accent-colored glyphs, and composite type selector buttons display each type's glyph with accent color on selection.
- [x] Reorder steps in the inspector steps section — The steps list in `BehaviourInspector.svelte` (line ~248, `{#each node.steps as step, i}`) supports add and remove but has no drag-to-reorder or move-up/move-down controls. Add reorder capability (e.g. up/down arrow buttons per step card, or drag handle) so users can rearrange `evaluate`/`instruct` steps within an action node. The section is `data-testid="tree-editor-right-dock-inspector-steps-section"`, each step card is `data-testid="…step-{i}"`. Will need a handler like `onMoveStep(fromIndex, toIndex)` that splices `node.steps` and triggers a store update. Key file: `packages/client/src/lib/tree/components/BehaviourInspector.svelte`.
- [ ] Add Playwright test suite — No Playwright dependency or config exists yet. Set up `@playwright/test` in the `packages/client` package (SvelteKit/Vite, Svelte 5). Add a `playwright.config.ts` at the monorepo root targeting the client dev server. Components already have broad `data-testid` coverage (~20 Svelte files). Start with smoke tests for core flows: open workspace, navigate between Collection/Designer/Marketplace, select a tree node and inspect it, add/remove steps in the inspector. Use the `.playwright/` folder for screenshots. Run via `bun` (the project's package manager). Key files for testid selectors: `App.svelte`, `TreeEditor.svelte`, `BehaviourInspector.svelte`, `ActivityBar.svelte`, `Home.svelte`.
- [x] Consolidate and refine workspace UX — The ActivityBar (`ActivityBar.svelte`) exposes Collection/Designer/Marketplace as top-level routes that are always navigable, even with no workspace open. This creates a hollow navigation experience (e.g. clicking Collection with no workspace just shows an empty card). Meanwhile the Designer's left rail (`BehaviourLeftRail.svelte`) already handles tree-to-tree switching well, making "Designer" feel like a page within the workspace rather than a peer of it. **Goal:** Make the workspace the primary container — opening a workspace should land the user in a cohesive view where collections and trees are accessible in-context, not as separate nav items. Consider: (1) gate or collapse nav items when no workspace is open, (2) merge the Collection grid into the Designer left rail or a workspace home pane, (3) keep Marketplace as a separate top-level route since it's independent of workspace state. Key files: `App.svelte` (routing), `ActivityBar.svelte` (nav), `Home.svelte` (collection grid), `BehaviourLeftRail.svelte` (tree switcher), `store.svelte.ts` (workspace state).